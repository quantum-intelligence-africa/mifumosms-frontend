# Netlify redirects for SPA (Single Page Application)
# Netlify automatically serves static files that exist, so we only need to redirect non-existent routes
# This ensures all routes are handled by index.html for client-side routing

# Handle all routes by serving index.html (static files are served automatically if they exist)
/*    /index.html   200
