import React from 'react';
import { SMSPackageCards } from '@/components/SMSPackageCards';

const SMSPackages = () => {
  return (
    <div className="min-h-screen bg-gray-50">
      <SMSPackageCards />
    </div>
  );
};

export default SMSPackages;
